import { LangSelectorBinPipe } from './lang-selector-bin.pipe';

describe('LangSelectorBinPipe', () => {
  it('create an instance', () => {
    const pipe = new LangSelectorBinPipe();
    expect(pipe).toBeTruthy();
  });
});
